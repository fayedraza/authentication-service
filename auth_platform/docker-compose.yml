version: "3.9"
services:
  auth-service:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./logs:/app/logs
    environment:
      - ENVIRONMENT=local
      - DEV_MODE=true
      - LOG_DIR=/app/logs

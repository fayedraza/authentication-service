"""
auth_service package

This package contains the core backend logic for the authentication service.
It includes:

- FastAPI application (`main.py`)
- SQLAlchemy models and database integration (`models.py`, `db.py`)
- Authentication and JWT logic (`auth.py`)
- Pydantic schemas (`schemas.py`)

Used as the entry point for the authentication microservice in the platform.
"""
